{
  "name": "crucible",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LAN Refresh Configuration Template Tool — Cisco IOS config migration helper",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.1"
  }
}
